/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/FWD_sum24',
  // output: 'export',
  reactStrictMode: true,
  images: {
    domains: ['imgs.xkcd.com'],
  },
};

export default nextConfig;
